import { SignUpSelectRoleCard } from '@/lib/auth';

const SignUpPage = () => (
    <div className="flex h-screen w-full flex-col items-center justify-center">
        <SignUpSelectRoleCard />
    </div>
);

export default SignUpPage;
