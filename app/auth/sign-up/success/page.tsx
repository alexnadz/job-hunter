import { SignUpSuccessCard } from '@/lib/auth';

const SignUpSuccessPage = () => {
    return (
        <div className="flex h-screen w-full flex-col items-center justify-center">
            <SignUpSuccessCard />
        </div>
    );
};

export default SignUpSuccessPage;
